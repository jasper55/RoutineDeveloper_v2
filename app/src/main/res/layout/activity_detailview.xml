<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/mainContainer">

    <data>
        <variable
            name="todo"
            type="com.example.app.jasper.routinedeveloperv2.model.Todo"/>
        <variable
            name="actions"
            type="com.example.app.jasper.routinedeveloperv2.view.DetailviewActions"/>
    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="10dp"
        >
        <!--android:layout_gravity="bottom|start"-->

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:weightSum="100"
            app:layout_constraintBottom_toBottomOf="parent">

            <LinearLayout
                android:layout_width="0dp"
                android:layout_weight="70"
                android:layout_height="wrap_content"
                android:orientation="vertical">
            <android.support.design.widget.TextInputLayout
                android:id="@+id/itemNameContainer"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:hint="Todo name">

                <android.support.design.widget.TextInputEditText
                    android:text="@={todo.name}"
                    android:textColor="@color/colorAccent"
                    android:inputType="textMultiLine"
                    android:id="@+id/ItemName"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content" />

            </android.support.design.widget.TextInputLayout>
            </LinearLayout>

            <LinearLayout
                android:layout_width="0dp"
                android:layout_weight="30"
                android:layout_height="wrap_content"
                app:layout_constraintBottom_toBottomOf="parent">

                <android.support.design.widget.FloatingActionButton
                    android:id="@+id/deleteItem"
                    app:srcCompat="@android:drawable/ic_menu_delete"
                    app:fabCustomSize="50dp"
                    android:tint="@color/colorPrimaryDark2"
                    android:layout_width="0dp"
                    android:layout_weight="15"
                    android:layout_height="wrap_content"
                    android:onClick="@{() -> actions.deleteTodo()}"
                    android:text="delete"
                    app:layout_constraintRight_toLeftOf="@id/saveItem"
                    app:layout_constraintBottom_toBottomOf="parent"
                    android:layout_marginBottom="10sp"
                    android:layout_marginLeft="10sp"/>

        <android.support.design.widget.FloatingActionButton
            android:id="@+id/saveItem"
            app:srcCompat="@android:drawable/ic_menu_save"
            app:fabCustomSize="50dp"
            android:tint="@color/colorPrimaryDark2"
            android:layout_width="0dp"
            android:layout_weight="15"
            android:layout_height="wrap_content"
            android:onClick="@{() -> actions.saveTodo()}"
            android:text="speichern"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginBottom="10sp"
            android:layout_marginLeft="10sp"/>



            </LinearLayout>

        </LinearLayout>

    </android.support.constraint.ConstraintLayout>
</layout>